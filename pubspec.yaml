name: skt_map_bug
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  skt:
    path: ./skt-maps

flutter:
  uses-material-design: true